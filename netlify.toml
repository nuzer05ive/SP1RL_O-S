[build]
  command   = "pnpm -F microsite run build"
  publish   = "apps/microsite/dist"
  base      = "apps/microsite"

[functions]
  directory = "netlify/functions"

[context.production.environment]
  NODE_ENV = "production"
