# SP1RL Prime Address & reMeMo.ri
**Address** = PP:<class>@φ43:<row>:<col>~W:<L|R>~K:<k>~N:<petal>~E:<epoch|->  
It is both a **label** and a **blueprint** (placement, color, polarity, epoch).  
Rebuild any shard from the address using `renderShardCard()`; palette supplies colors/glyphs.
